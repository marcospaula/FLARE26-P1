# --- Ambiente Python ---
.venv/
venv/
__pycache__/
*.pyc

# --- Segredos (NUNCA versionar) ---
.env
.streamlit/secrets.toml

# --- Artefatos de runtime (regeneráveis) ---
chroma_db/
*.db
flare26_cache_docs.db

# --- Ledgers gerados em execução ---
# O ledger é mutável a cada auditoria; o dataset curado de testes
# vive em tests/fixtures, não na raiz.
flare26_ledger.json
flare26_ledger_v2.json

# --- Artefatos derivados do paper (regenerados de draft.md/.tex) ---
paper/draft.pdf
paper/draft.odt
paper/draft.txt
paper/*.aux
paper/*.log
paper/*.out
